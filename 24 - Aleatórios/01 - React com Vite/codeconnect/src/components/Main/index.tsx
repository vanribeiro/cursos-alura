import Filter from '../Filter';
import SearchBar from '../SearchBar';
import Sorting from '../Sorting';

function Main() {
    return(
        <main>
            <SearchBar />
            <Filter />
            <Sorting />
        </main>
    );
}

export default Main;