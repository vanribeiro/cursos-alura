import Filter from '../Filter';
import SearchBar from '../SearchBar';

function Main() {
    return(
        <main>
            <SearchBar />
            <Filter />
        </main>
    );
}

export default Main;